{
  "name": "oman-data-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync": "node scripts/sync-data.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "astro": "^7.0.0"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}
